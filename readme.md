1. **Ввод:** Пользователь заходит на главную страницу (/), где видит форму с двумя элементами:
    - Поле для загрузки файла (.xlsx).
    - Дополнительное поле ввода (число или текст) — *параметр для расчета* (зависит от варианта).
2. **Обработка:** Сервер принимает файл, читает его через pandas, производит вычисления с учетом введенного параметра.
3. **Вывод:** Браузер автоматически предлагает скачать готовый файл (report.xlsx).

### 🔹 Вариант 5: Отчет по продажам за период

**Сценарий:** Директор загружает общую историю продаж и хочет получить отчет только по крупным сделкам выше определенной суммы.

-   **Входной файл:**
    -   Дата (строка или дата)
    -   Менеджер (строка)
    -   Сумма сделки (число)
-   **Параметр в форме:** Минимальная сумма сделки (число, например, 10000).
-   **Задание на обработку:**
    1. Отфильтровать таблицу: оставить только сделки, где Сумма >= введенного параметра.
    2. Рассчитать столбец Бонус менеджера (10% от суммы сделки).
    3. Сделать **второй лист** в Excel-файле (Sheet2) под названием "Сводка", где вывести таблицу: Менеджер | Сумма всех его продаж.
-   **Итоговый файл:** Файл .xlsx с двумя листами (Детализация и Сводка).
